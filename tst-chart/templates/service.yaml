apiVersion: v1
kind: Service
metadata:
  name: {{ include "tst-chart.fullname" . }}
  labels:
    {{- include "tst-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tst-chart.selectorLabels" . | nindent 4 }}
